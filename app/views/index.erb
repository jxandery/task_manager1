<h1>All Tasks</h1>

<% @tasks.each do |task| %>     # inside of the view, we will iterate through the array @tasks and display each string
  <h3><%= task.id %>: <a href="/tasks/<%= task.id %>"><%= task.title %></a></h3>
<% end %>
